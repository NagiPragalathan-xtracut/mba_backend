# ---------------------------------------------------------------------------
# MBU Backend - Python dependencies
# Keep this file in sync with the code: add a package the moment it is used.
# ---------------------------------------------------------------------------

# Core framework
Django==5.2.12
django-environ==0.13.0

# Admin UI
django-unfold==0.74.1

# Rich text editing
django-ckeditor==6.7.0
django-js-asset==3.1.2

# REST API
djangorestframework==3.17.1
django-filter==25.2
django-cors-headers==4.9.0
drf-spectacular==0.29.0

# Database drivers
# SQLite needs nothing; mysqlclient is Django's supported MySQL driver.
mysqlclient==2.2.8

# Media handling
pillow==12.0.0

# Media storage on S3 (enabled with USE_S3=True; boto3 is the AWS client)
django-storages==1.14.6
boto3==1.40.47

# MCP server (Claude integration)
mcp==1.27.0
httpx==0.28.1

# Production server (optional, used by the deployment docs)
gunicorn==23.0.0 ; sys_platform != "win32"
whitenoise==6.9.0
