"""
Settings package.

Import a concrete module rather than this package:

    DJANGO_SETTINGS_MODULE=mbu_backend.settings.development
    DJANGO_SETTINGS_MODULE=mbu_backend.settings.production

`manage.py`, `wsgi.py` and `asgi.py` resolve the module from the `DJANGO_ENV`
environment variable and default to `development`.
"""
